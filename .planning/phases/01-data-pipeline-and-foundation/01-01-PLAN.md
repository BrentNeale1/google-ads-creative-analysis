---
phase: 01-data-pipeline-and-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - tsconfig.json
  - tailwind.config.ts
  - next.config.mjs
  - postcss.config.mjs
  - app/layout.tsx
  - app/page.tsx
  - app/globals.css
  - components/layout/Sidebar.tsx
  - components/layout/AppShell.tsx
  - lib/constants/colours.ts
  - lib/constants/formatting.ts
autonomous: true
requirements:
  - VIS-03
  - DASH-06
  - DASH-07

must_haves:
  truths:
    - "Next.js dev server starts without errors on port 3000"
    - "Tailwind design system colours from CLAUDE.md are available as utility classes (e.g. bg-brand-blue, text-brand-red)"
    - "App renders a left sidebar with navigation sections: Dashboard, RSA Analysis, PMax, Display, Video, Settings"
    - "Layout is responsive: sidebar collapses or adapts on tablet widths (below lg breakpoint)"
    - "Setup guide empty state displays on the home page with welcome message and connection steps"
    - "AU locale formatting utilities produce correct output: $1,234.56 for currency, 3.2% for percentages, $12.4K for compact"
  artifacts:
    - path: "tailwind.config.ts"
      provides: "Design system colour tokens from CLAUDE.md"
      contains: "brand-blue.*#1A73E8"
    - path: "components/layout/Sidebar.tsx"
      provides: "Left sidebar navigation with all section links"
      min_lines: 40
    - path: "components/layout/AppShell.tsx"
      provides: "Main layout wrapper with sidebar and content area"
      min_lines: 20
    - path: "lib/constants/formatting.ts"
      provides: "AU locale number formatting utilities"
      exports: ["formatCurrency", "formatCurrencyCompact", "formatPercentage", "formatNumber", "formatNumberCompact"]
    - path: "lib/constants/colours.ts"
      provides: "Design system colour tokens as JS constants"
      exports: ["COLOURS"]
    - path: "app/page.tsx"
      provides: "Setup guide empty state for first-time visitors"
      min_lines: 20
  key_links:
    - from: "app/layout.tsx"
      to: "components/layout/AppShell.tsx"
      via: "import and render as root wrapper"
      pattern: "import.*AppShell"
    - from: "components/layout/AppShell.tsx"
      to: "components/layout/Sidebar.tsx"
      via: "import and render sidebar alongside children"
      pattern: "import.*Sidebar"
    - from: "tailwind.config.ts"
      to: "CLAUDE.md colour palette"
      via: "theme.extend.colors configuration"
      pattern: "brand.*blue.*#1A73E8"
---

<objective>
Create the Next.js 14 project scaffold with Tailwind CSS design system, responsive app shell with sidebar navigation, AU locale formatting utilities, and setup guide empty state.

Purpose: Establishes the project foundation that all subsequent plans build upon -- the running app, design system tokens, layout structure, and formatting utilities.
Output: A working Next.js app with sidebar navigation, design system colours, formatting utilities, and a welcoming empty state.
</objective>

<execution_context>
@C:/Users/brent/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/brent/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-data-pipeline-and-foundation/01-RESEARCH.md
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Scaffold Next.js project with Tailwind design system and formatting utilities</name>
  <files>
    package.json
    tsconfig.json
    tailwind.config.ts
    next.config.mjs
    postcss.config.mjs
    app/globals.css
    lib/constants/colours.ts
    lib/constants/formatting.ts
  </files>
  <action>
    1. Run `npx create-next-app@14 . --typescript --tailwind --app --src-dir=false --import-alias "@/*" --use-npm` in the project root (which currently has only CLAUDE.md and .planning/). If create-next-app refuses to run in a non-empty directory, use a temp directory then move files back. Do NOT use --src-dir (project structure in RESEARCH.md puts app/ at root, not src/app/).

    2. Install production dependencies:
       `npm install drizzle-orm @neondatabase/serverless zod nanoid lucide-react`

    3. Install dev dependencies:
       `npm install -D drizzle-kit dotenv`

    4. Configure Tailwind in `tailwind.config.ts` with the CLAUDE.md design system colours:
       - brand.blue: #1A73E8, brand.green: #34A853, brand.red: #EA4335, brand.grey: #9AA0A6, brand.amber: #FBBC04
       - surface.background: #F8F9FA, surface.gridline: #E8EAED, surface.table-header: #F1F3F4, surface.row-positive: #E6F4EA, surface.row-negative: #FCE8E6
       - Content paths: ./app/**/*.{ts,tsx,mdx}, ./components/**/*.{ts,tsx,mdx}, ./lib/**/*.{ts,tsx,mdx}
       See exact config in 01-RESEARCH.md "Tailwind Design System Colour Configuration" section.

    5. Create `lib/constants/colours.ts` exporting a COLOURS object with all design system tokens as a typed constant (for use in Recharts and other JS contexts where Tailwind classes don't apply):
       ```typescript
       export const COLOURS = {
         brand: { blue: '#1A73E8', green: '#34A853', red: '#EA4335', grey: '#9AA0A6', amber: '#FBBC04' },
         surface: { background: '#F8F9FA', gridline: '#E8EAED', tableHeader: '#F1F3F4', rowPositive: '#E6F4EA', rowNegative: '#FCE8E6' },
       } as const;
       ```

    6. Create `lib/constants/formatting.ts` with AU locale number formatting utilities using Intl.NumberFormat('en-AU'):
       - formatCurrency(value: number): string -- AUD $1,234.56 format
       - formatCurrencyCompact(value: number): string -- $12.4K format (notation: 'compact')
       - formatPercentage(value: number): string -- 3.2% format (note: input is a ratio like 0.032, NOT already multiplied by 100)
       - formatNumber(value: number): string -- 1,234 format
       - formatNumberCompact(value: number): string -- 12.4K format
       - convertMicrosToAud(micros: number): number -- divides by 1,000,000 for cost_micros conversion
       All named exports per CLAUDE.md code style.
       See exact implementations in 01-RESEARCH.md "AU Locale Number Formatting Utilities" section.

    7. Update `app/globals.css` to set body background to surface.background (#F8F9FA) and ensure clean base styles. Keep Tailwind directives (@tailwind base/components/utilities). Add minimal resets only.
  </action>
  <verify>
    <automated>cd C:/Users/brent/google-ads-analyser && npm run build 2>&1 | tail -5</automated>
    <manual>Verify tailwind.config.ts contains all CLAUDE.md colour tokens</manual>
  </verify>
  <done>
    - Next.js 14 project builds without errors
    - All npm dependencies installed (drizzle-orm, @neondatabase/serverless, zod, nanoid, lucide-react, drizzle-kit, dotenv)
    - Tailwind config has all CLAUDE.md design system colours as custom tokens
    - Formatting utilities export all 5 formatters + convertMicrosToAud with correct AU locale output
    - COLOURS constant exports all design system tokens for JS usage
  </done>
</task>

<task type="auto">
  <name>Task 2: Build responsive app shell with sidebar navigation and setup guide empty state</name>
  <files>
    app/layout.tsx
    app/page.tsx
    components/layout/Sidebar.tsx
    components/layout/AppShell.tsx
  </files>
  <action>
    1. Create `components/layout/Sidebar.tsx`:
       - Left sidebar with navigation links: Dashboard (home icon), RSA Analysis, PMax, Display, Video, Settings (gear icon)
       - Use lucide-react icons for each section (LayoutDashboard, Search, Zap, Monitor, PlayCircle, Settings or similar appropriate icons)
       - Linear-style aesthetic: clean, minimal chrome, subtle hover states, no heavy borders
       - Active link highlighted with brand.blue (#1A73E8) indicator
       - Sections beyond Dashboard and Settings are "coming soon" -- render them with reduced opacity (opacity-50) and cursor-not-allowed since those phases haven't shipped yet
       - Responsive behaviour: on screens below lg breakpoint (1024px), sidebar collapses to icon-only or hides behind a hamburger menu toggle. On lg+ screens, full sidebar with labels visible.
       - Width: ~240px expanded, ~64px collapsed (icon-only)
       - Background: white or very light (#FFFFFF), subtle right border (#E8EAED)
       - Bottom of sidebar: small "Creative Analyser" text, minimal/unbranded per user decision

    2. Create `components/layout/AppShell.tsx`:
       - Wrapper component that renders Sidebar on the left and {children} in the main content area
       - Main content area: flex-1, overflow-y-auto, padding for comfortable reading (p-6 on lg, p-4 on smaller)
       - Background of content area: surface.background (#F8F9FA)
       - Responsive: sidebar and content area use flex layout

    3. Update `app/layout.tsx`:
       - Import and wrap children with AppShell
       - Set metadata: title "Creative Analyser", description "Google Ads creative performance analysis"
       - Use Inter font from next/font/google (or the default sans-serif font from create-next-app)
       - Apply bg-surface-background to body

    4. Create `app/page.tsx` with setup guide empty state:
       - Heading: "Welcome to Creative Analyser"
       - Subheading: "Surface what's working, what's not, and what to test next"
       - Step-by-step guide displayed as numbered cards:
         1. "Set up Google Ads Script" -- Copy the provided script into your Google Ads account
         2. "Configure API Key" -- Add your unique API key to the script configuration
         3. "Push First Data" -- Run the script manually or wait for the daily schedule
         4. "View Your Dashboard" -- Performance data will appear here automatically
       - Connection status indicator: "No data received yet" with a neutral grey badge
       - Clean card layout with subtle shadows, brand.blue accent on step numbers
       - Use Tailwind utility classes only, no custom CSS. Follow the Linear-style aesthetic.
       - All copy in AU English spelling (analyse, optimise, etc.)
  </action>
  <verify>
    <automated>cd C:/Users/brent/google-ads-analyser && npm run dev -- --port 3099 &amp; sleep 5 &amp;&amp; curl -s http://localhost:3099 | head -20; kill %1 2>/dev/null</automated>
    <manual>Open http://localhost:3000 -- verify sidebar renders with all sections, layout is responsive (resize browser), setup guide shows on home page</manual>
  </verify>
  <done>
    - App shell renders with left sidebar containing all 6 navigation sections (Dashboard, RSA Analysis, PMax, Display, Video, Settings)
    - Non-shipped sections (RSA Analysis, PMax, Display, Video) appear greyed out / disabled
    - Layout is responsive: sidebar adapts below lg breakpoint
    - Home page shows setup guide empty state with 4 numbered steps and "No data received yet" status
    - Linear-style aesthetic: clean, minimal chrome, no heavy decoration
    - All copy uses AU English spelling
  </done>
</task>

</tasks>

<verification>
1. `npm run build` completes without errors
2. `npm run dev` starts dev server on port 3000
3. Home page renders setup guide with 4 steps
4. Sidebar shows all 6 navigation sections
5. Resizing browser below 1024px adapts sidebar layout
6. `lib/constants/formatting.ts` exports all 5 formatters producing correct AU locale output
7. Tailwind config includes all CLAUDE.md colour tokens
</verification>

<success_criteria>
- Next.js 14 app runs locally with zero errors
- Design system colours are configured and available as Tailwind utilities
- Responsive sidebar navigation renders with all planned sections
- AU locale formatting utilities produce correct output
- Setup guide empty state provides clear onboarding path
- All code follows CLAUDE.md style: AU English, named exports, Tailwind utilities, PascalCase components, camelCase utilities
</success_criteria>

<output>
After completion, create `.planning/phases/01-data-pipeline-and-foundation/01-01-SUMMARY.md`
</output>
