---
phase: 04-multi-format-analysis-and-intelligence
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/queries/pmax.ts
  - app/pmax/page.tsx
  - app/pmax/searchParams.ts
  - components/pmax/PmaxTabNav.tsx
  - components/pmax/PmaxLeaderboard.tsx
  - components/pmax/PmaxThemeAnalysis.tsx
autonomous: true
requirements:
  - PMAX-01
  - PMAX-02
  - PMAX-03
  - PMAX-04
  - PMAX-05
  - PMAX-06

must_haves:
  truths:
    - "PMax asset groups are classified into top 20%, middle 60%, bottom 20% tiers by primary KPI"
    - "PMax page shows asset group leaderboard sorted by performance with portfolio average"
    - "PMax page shows theme analysis for top-performing asset groups based on text assets"
    - "PMax page shows underperformer diagnosis with evidence and recommended actions"
    - "PMax page shows Keep/Test/Pause/Investigate recommendations"
    - "PMax page displays prominent caveat about Google controlling creative assembly"
  artifacts:
    - path: "lib/queries/pmax.ts"
      provides: "PMax data aggregation queries"
      exports: ["fetchPmaxAssetGroups", "fetchPmaxPortfolioAvg", "fetchPmaxTextAssets"]
    - path: "app/pmax/page.tsx"
      provides: "PMax analysis page Server Component"
      exports: ["default"]
    - path: "app/pmax/searchParams.ts"
      provides: "PMax URL search param cache"
      exports: ["pmaxSearchParams", "resolveDateRange"]
    - path: "components/pmax/PmaxLeaderboard.tsx"
      provides: "PMax asset group leaderboard table"
      exports: ["PmaxLeaderboard"]
    - path: "components/pmax/PmaxThemeAnalysis.tsx"
      provides: "PMax theme vs performance analysis"
      exports: ["PmaxThemeAnalysis"]
  key_links:
    - from: "app/pmax/page.tsx"
      to: "lib/queries/pmax.ts"
      via: "Server-side data fetch"
      pattern: "fetchPmaxAssetGroups"
    - from: "app/pmax/page.tsx"
      to: "lib/analysis/tierClassification.ts"
      via: "classifyTiers pipeline"
      pattern: "classifyTiers"
    - from: "app/pmax/page.tsx"
      to: "lib/analysis/recommendations.ts"
      via: "generateRecommendations pipeline"
      pattern: "generateRecommendations"
---

<objective>
Build the PMax analysis page with full asset group analysis: tier classification, leaderboard, theme analysis, underperformer diagnosis, pattern detection, and recommendations.

Purpose: PMax is the second most important campaign type after RSA. Asset group level analysis shows which creative themes drive performance, with explicit caveats that Google controls creative assembly.
Output: Working /pmax page with 3 tabs (Overview, Asset Groups, Recommendations) following the RSA page pattern exactly.
</objective>

<execution_context>
@C:/Users/brent/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/brent/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-multi-format-analysis-and-intelligence/04-RESEARCH.md
@.planning/phases/03-rsa-analysis/03-03-SUMMARY.md
@.planning/phases/03-rsa-analysis/03-04-SUMMARY.md
@app/rsa/page.tsx
@app/rsa/searchParams.ts
@lib/queries/rsa.ts
@lib/db/schema.ts
@lib/analysis/types.ts
@components/rsa/TierOverview.tsx
@components/rsa/CreativeLeaderboard.tsx
@components/rsa/UnderperformerPanel.tsx
@components/rsa/PatternCharts.tsx
@components/rsa/RecommendationList.tsx
@components/rsa/RsaTabNav.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: PMax query module and search params</name>
  <files>lib/queries/pmax.ts, app/pmax/searchParams.ts</files>
  <action>
**Create `lib/queries/pmax.ts`** following the exact pattern from `lib/queries/rsa.ts`:
- Add `import "server-only"` at top
- Import db, schema, drizzle operators (eq, and, gte, lte, sum, desc), and `convertMicrosToAud` from formatting
- Define `PmaxAssetGroupAggregated` interface: assetGroupId, assetGroupName, campaignName, impressions, clicks, costMicros, conversions, conversionsValue, costAud, ctr, cvr, cpa, roas
- Add `safeDivide(n, d)` helper (zero-division guard)
- Export `fetchPmaxAssetGroups(accountId, dateFrom, dateTo, campaignId?)`: aggregate `pmaxAssetGroupDaily` by assetGroupId+assetGroupName+campaignName, compute derived metrics (costAud, ctr, cvr, cpa, roas) with safeDivide. Optional campaignId filter.
- Export `fetchPmaxPortfolioAvg(accountId, dateFrom, dateTo)`: compute portfolio-level averages (avgImpressions, avgCtr, avgCvr) following the same pattern as `fetchRsaPortfolioAvg` -- aggregate per-creative first, then average across creatives.
- Export `fetchPmaxTextAssets(accountId, dateFrom, dateTo)`: query `pmaxAssetDaily` where assetType='TEXT', group by assetGroupId+textContent, return array of `{ assetGroupId, textContent, performanceLabel }`. This provides headline text for pattern detection on PMax creatives.

**Create `app/pmax/searchParams.ts`** following the exact pattern from `app/rsa/searchParams.ts`:
- Import createSearchParamsCache, parseAsString, parseAsStringLiteral, parseAsIsoDate from "nuqs/server"
- Import subDays, format, startOfDay from "date-fns"
- Define `pmaxSearchParams` cache with: account, range (7d/30d/90d/custom), from, to, campaign (no adGroup -- PMax uses asset groups not ad groups), tab (overview/assets/recommendations)
- Export `resolveDateRange` function (copy from rsa/searchParams.ts -- identical logic)
- Export type aliases: `PmaxTab`, `DateRange`
  </action>
  <verify>
    <automated>npx tsc --noEmit --pretty 2>&1 | head -30</automated>
    <manual>Verify lib/queries/pmax.ts has server-only import and correct Drizzle schema references</manual>
  </verify>
  <done>PMax query module exports 3 functions with proper type safety, zero-division guards, and server-only protection. Search params cache defined for PMax page.</done>
</task>

<task type="auto">
  <name>Task 2: PMax analysis page with all UI components</name>
  <files>app/pmax/page.tsx, components/pmax/PmaxTabNav.tsx, components/pmax/PmaxLeaderboard.tsx, components/pmax/PmaxThemeAnalysis.tsx</files>
  <action>
**Create `components/pmax/PmaxTabNav.tsx`** -- "use client" component:
- Follow the exact pattern from `components/rsa/RsaTabNav.tsx`
- 3 tabs: Overview, Asset Groups, Recommendations
- Uses `useQueryStates` with `parseAsStringLiteral` for tab param

**Create `components/pmax/PmaxLeaderboard.tsx`** -- "use client" component:
- Follow the `CreativeLeaderboard` pattern but adapted for PMax:
- Shows asset group name (not headline text) in the first column
- Shows campaign name as secondary info
- Columns: Asset Group, Campaign, Impr., Clicks, CTR, Conv., CPA/ROAS (depending on kpiType)
- Top 5 / Bottom 5 tables with tier-highlighted rows (green/red per design system)
- Portfolio average row at bottom of each table
- Create formatKpi inside the component from kpiType prop (Server/Client boundary rule)

**Create `components/pmax/PmaxThemeAnalysis.tsx`** -- "use client" component:
- Shows which text themes are present across PMax asset groups
- Uses a simple table/card layout showing asset group name + text assets + performance label
- Add prominent caveat banner at top: "Google controls creative assembly in Performance Max campaigns. Theme analysis reflects asset composition, not Google's served combinations."

**Create `app/pmax/page.tsx`** -- Server Component following the exact RSA page pattern:
1. Parse pmaxSearchParams from searchParams
2. If no account selected, show prompt (same empty state as RSA page but with PMax-specific text)
3. Fetch account record for primaryKpi
4. resolveDateRange from params
5. Fetch data in parallel: `fetchPmaxAssetGroups`, `fetchPmaxPortfolioAvg`, `fetchPmaxTextAssets`, `fetchFilterOptions`
6. Enrich asset groups as `CreativeInput[]`:
   - adId = assetGroupId
   - kpiValue = primaryKpi === 'cpa' ? ag.cpa : ag.roas
   - headlineText = join text from fetchPmaxTextAssets for this assetGroupId (filter textAssets by assetGroupId, join textContent with " | ")
   - Pass through metrics (impressions, clicks, ctr, conversions, cpa, roas, campaignName, assetGroupName)
7. Run shared analysis pipeline: classifyTiers, diagnoseUnderperformers, detectPatterns, generateRecommendations, generateInsightTitle
8. Render with FilterBar (campaigns only, no adGroups for PMax), PmaxTabNav, and tab content:
   - Overview tab: reuse RSA `TierOverview` and `PatternCharts` components (they accept generic props). Use `PmaxLeaderboard` for the leaderboard. Add caveat banner at top of overview.
   - Asset Groups tab: `PmaxThemeAnalysis` showing text assets by asset group
   - Recommendations tab: reuse RSA `UnderperformerPanel` and `RecommendationList` components
9. Add prominent PMax caveat banner at page top: "Performance Max: Google controls creative assembly. Data is directional -- it reflects Google's optimisation choices, not controlled creative tests."
10. Export `dynamic = "force-dynamic"`

**IMPORTANT patterns to follow:**
- formatKpi must be created inside each Client Component from kpiType prop (not passed as function prop)
- AU English spelling in all UI copy (analyse, optimise, colour, etc.)
- Design system colours: green #34A853 for top, amber #FBBC04 for middle, red #EA4335 for bottom
- Insight-led chart titles from generateInsightTitle
  </action>
  <verify>
    <automated>npx tsc --noEmit --pretty 2>&1 | head -30</automated>
    <manual>Navigate to /pmax?account={seed-account-id} and verify tier overview, leaderboard, theme analysis, and recommendations render correctly with PMax caveat banner visible</manual>
  </verify>
  <done>PMax page at /pmax renders tier overview, asset group leaderboard, theme analysis, underperformer diagnosis, pattern charts, and recommendations. Caveat banner prominently displayed. All 3 tabs work via URL param navigation.</done>
</task>

</tasks>

<verification>
```bash
npx tsc --noEmit --pretty
```
TypeScript compilation passes with no errors.

```bash
npx vitest run --reporter=verbose
```
All existing tests still pass (no regressions).

Visual verification: /pmax page loads with seed data, shows tier classification, leaderboard with asset group names, and PMax caveat banner.
</verification>

<success_criteria>
- /pmax page renders with seed data showing PMax asset group analysis
- Tier classification works (top 20% / middle 60% / bottom 20%)
- Leaderboard shows asset group names, not headline text
- PMax caveat banner is prominently displayed
- 3 tabs navigate via URL search param
- Reuses existing analysis functions (classifyTiers, diagnoseUnderperformers, detectPatterns, generateRecommendations)
- No new npm packages
</success_criteria>

<output>
After completion, create `.planning/phases/04-multi-format-analysis-and-intelligence/04-02-SUMMARY.md`
</output>
