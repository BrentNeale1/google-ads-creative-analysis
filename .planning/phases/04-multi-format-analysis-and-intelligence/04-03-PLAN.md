---
phase: 04-multi-format-analysis-and-intelligence
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/queries/display.ts
  - app/display/page.tsx
  - app/display/searchParams.ts
  - components/display/DisplayTabNav.tsx
  - components/display/DisplayLeaderboard.tsx
  - components/display/FormatComparison.tsx
autonomous: true
requirements:
  - DISP-01
  - DISP-02
  - DISP-03
  - DISP-04
  - DISP-05
  - DISP-06

must_haves:
  truths:
    - "Display creatives are classified into top 20%, middle 60%, bottom 20% tiers by primary KPI"
    - "Display page shows creative leaderboard with ad name and ad type badge"
    - "Display page shows format comparison chart grouping performance by ad type"
    - "Display page shows underperformer diagnosis with evidence and recommended actions"
    - "Display page shows Keep/Test/Pause/Investigate recommendations"
  artifacts:
    - path: "lib/queries/display.ts"
      provides: "Display data aggregation queries"
      exports: ["fetchDisplayCreatives", "fetchDisplayPortfolioAvg", "fetchDisplayFormatBreakdown"]
    - path: "app/display/page.tsx"
      provides: "Display analysis page Server Component"
      exports: ["default"]
    - path: "app/display/searchParams.ts"
      provides: "Display URL search param cache"
      exports: ["displaySearchParams", "resolveDateRange"]
    - path: "components/display/FormatComparison.tsx"
      provides: "Ad type format comparison chart"
      exports: ["FormatComparison"]
  key_links:
    - from: "app/display/page.tsx"
      to: "lib/queries/display.ts"
      via: "Server-side data fetch"
      pattern: "fetchDisplayCreatives"
    - from: "app/display/page.tsx"
      to: "lib/analysis/tierClassification.ts"
      via: "classifyTiers pipeline"
      pattern: "classifyTiers"
    - from: "components/display/FormatComparison.tsx"
      to: "recharts"
      via: "Horizontal bar chart for format comparison"
      pattern: "BarChart|Bar"
---

<objective>
Build the Display/Demand Gen analysis page with tier classification, leaderboard, format comparison by ad type, underperformer diagnosis, and recommendations.

Purpose: Display ads are the second format type. The key unique feature is DISP-03: format comparison by ad type (RESPONSIVE_DISPLAY_AD, IMAGE_AD, DISCOVERY_MULTI_ASSET_AD) since image dimensions are not available in the schema.
Output: Working /display page with 3 tabs (Overview, Formats, Recommendations) following the RSA page pattern.
</objective>

<execution_context>
@C:/Users/brent/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/brent/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-multi-format-analysis-and-intelligence/04-RESEARCH.md
@.planning/phases/03-rsa-analysis/03-03-SUMMARY.md
@.planning/phases/03-rsa-analysis/03-04-SUMMARY.md
@app/rsa/page.tsx
@app/rsa/searchParams.ts
@lib/queries/rsa.ts
@lib/db/schema.ts
@lib/analysis/types.ts
@components/rsa/TierOverview.tsx
@components/rsa/PatternCharts.tsx
@components/rsa/UnderperformerPanel.tsx
@components/rsa/RecommendationList.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Display query module and search params</name>
  <files>lib/queries/display.ts, app/display/searchParams.ts</files>
  <action>
**Create `lib/queries/display.ts`** following the exact pattern from `lib/queries/rsa.ts`:
- Add `import "server-only"` at top
- Import db, schema, drizzle operators, convertMicrosToAud
- Define `DisplayCreativeAggregated` interface: adId, adName (nullable), adType, campaignName, adGroupName, impressions, clicks, costMicros, conversions, conversionsValue, costAud, ctr, cvr, cpa, roas
- Add `safeDivide(n, d)` helper
- Export `fetchDisplayCreatives(accountId, dateFrom, dateTo, campaignId?, adGroupId?)`: aggregate `displayDaily` by adId+adName+adType+campaignName+adGroupName, compute derived metrics. Optional campaignId and adGroupId filters.
- Export `fetchDisplayPortfolioAvg(accountId, dateFrom, dateTo)`: compute portfolio-level averages (avgImpressions, avgCtr, avgCvr) following the per-creative-then-average pattern.
- Export `fetchDisplayFormatBreakdown(accountId, dateFrom, dateTo)`: aggregate `displayDaily` by adType only, returning `{ adType, impressions, clicks, ctr, conversions, cpa, roas }[]` for each ad type. This provides the data for DISP-03 format comparison.
  - Map adType values to human-readable labels in the query result: RESPONSIVE_DISPLAY_AD -> "Responsive Display", IMAGE_AD -> "Image Ad", DISCOVERY_MULTI_ASSET_AD -> "Discovery Multi-Asset"

**Create `app/display/searchParams.ts`** following the RSA pattern:
- Import createSearchParamsCache, parseAsString, parseAsStringLiteral, parseAsIsoDate from "nuqs/server"
- Import date-fns helpers
- Define `displaySearchParams` cache with: account, range, from, to, campaign, adGroup, tab (overview/formats/recommendations)
- Export `resolveDateRange` function (identical logic)
- Export type aliases: `DisplayTab`, `DateRange`
  </action>
  <verify>
    <automated>npx tsc --noEmit --pretty 2>&1 | head -30</automated>
  </verify>
  <done>Display query module exports 3 functions with proper types, zero-division guards, server-only protection, and human-readable ad type labels. Search params defined for Display page.</done>
</task>

<task type="auto">
  <name>Task 2: Display analysis page with format comparison</name>
  <files>app/display/page.tsx, components/display/DisplayTabNav.tsx, components/display/DisplayLeaderboard.tsx, components/display/FormatComparison.tsx</files>
  <action>
**Create `components/display/DisplayTabNav.tsx`** -- "use client" component:
- Follow RsaTabNav pattern with 3 tabs: Overview, Formats, Recommendations

**Create `components/display/DisplayLeaderboard.tsx`** -- "use client" component:
- Adapted from CreativeLeaderboard for Display:
- First column shows ad name (or "Ad {adId}" fallback) with ad type badge (small coloured pill)
- Ad type badges: "Responsive Display" = blue, "Image Ad" = grey, "Discovery" = amber (use design system colours)
- Columns: Creative, Type, Impr., Clicks, CTR, Conv., CPA/ROAS
- Top 5 / Bottom 5 tables with tier highlighting
- Portfolio average row
- formatKpi created inside component from kpiType prop

**Create `components/display/FormatComparison.tsx`** -- "use client" component:
- This is the unique DISP-03 component
- Accepts `formatBreakdown: { adType: string; impressions: number; clicks: number; ctr: number; conversions: number; cpa: number; roas: number }[]` and `kpiType` props
- Renders a **horizontal bar chart** using Recharts comparing the primary KPI across ad types
- Chart follows design system rules:
  - Sorted by KPI value descending (for ROAS) or ascending (for CPA, lower is better)
  - Data labels on bars (fewer than 10 items guaranteed)
  - No borders, no 3D, subtle gridlines (#E8EAED) or none
  - Background #F8F9FA
  - Primary Blue #1A73E8 for bars
- Below the chart, render a summary table with all metrics per format
- Insight-led title computed from data: e.g. "Responsive Display ads achieve 23% lower CPA" or "Image Ads deliver 1.4x higher ROAS"
- formatKpi created inside component from kpiType prop

**Create `app/display/page.tsx`** -- Server Component following RSA page pattern:
1. Parse displaySearchParams
2. Empty state if no account selected
3. Fetch account for primaryKpi
4. resolveDateRange
5. Fetch in parallel: fetchDisplayCreatives, fetchDisplayPortfolioAvg, fetchDisplayFormatBreakdown, fetchFilterOptions
6. Enrich creatives as CreativeInput[]:
   - adId = c.adId
   - kpiValue = primaryKpi === 'cpa' ? c.cpa : c.roas
   - headlineText = undefined (Display ads don't have headline text -- per research, skip text-based pattern detection)
   - Pass through: impressions, clicks, ctr, conversions, cpa, roas, campaignName, adGroupName, adName, adType
7. Run analysis pipeline: classifyTiers, diagnoseUnderperformers, generateRecommendations
   - NOTE: detectPatterns will return limited results since headlineText is empty. This is expected for Display.
   - For middle-tier creatives without text, recommendations engine will default to "monitor" per research guidance.
8. Render with FilterBar (campaigns + ad groups), DisplayTabNav, tab content:
   - Overview tab: TierOverview (reused) + DisplayLeaderboard
   - Formats tab: FormatComparison chart and table
   - Recommendations tab: UnderperformerPanel (reused) + RecommendationList (reused)
9. Export `dynamic = "force-dynamic"`

**Display-specific notes:**
- No pattern charts on overview tab (Display lacks headline text for theme detection)
- Format comparison is the unique analytical feature for Display
- AU English in all UI copy
  </action>
  <verify>
    <automated>npx tsc --noEmit --pretty 2>&1 | head -30</automated>
    <manual>Navigate to /display?account={seed-account-id} and verify tier overview, leaderboard with ad type badges, format comparison chart, and recommendations render correctly</manual>
  </verify>
  <done>Display page at /display renders tier overview, creative leaderboard with ad type badges, format comparison horizontal bar chart, underperformer diagnosis, and recommendations. All 3 tabs work via URL param navigation.</done>
</task>

</tasks>

<verification>
```bash
npx tsc --noEmit --pretty
```
TypeScript compilation passes with no errors.

```bash
npx vitest run --reporter=verbose
```
All existing tests still pass (no regressions).

Visual verification: /display page loads with seed data, shows format comparison chart with ad type breakdown.
</verification>

<success_criteria>
- /display page renders with seed data showing Display creative analysis
- Tier classification works (top 20% / middle 60% / bottom 20%)
- Leaderboard shows ad name with ad type badge
- Format comparison chart (DISP-03) shows horizontal bars comparing ad types by KPI
- Pattern detection gracefully handles missing headline text (no crash, shows limited results)
- 3 tabs navigate via URL search param
- No new npm packages
</success_criteria>

<output>
After completion, create `.planning/phases/04-multi-format-analysis-and-intelligence/04-03-SUMMARY.md`
</output>
