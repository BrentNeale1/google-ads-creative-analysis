---
phase: 04-multi-format-analysis-and-intelligence
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - lib/analysis/gapAnalysis.ts
  - lib/analysis/gapAnalysis.test.ts
  - lib/analysis/fatigueDetection.ts
  - lib/analysis/fatigueDetection.test.ts
  - lib/analysis/videoAnalysis.ts
  - lib/analysis/videoAnalysis.test.ts
autonomous: true
requirements:
  - INTL-01
  - INTL-02
  - VID-04

must_haves:
  truths:
    - "identifyGaps returns themes with zero or minimal representation in the portfolio"
    - "detectFatigue flags creatives whose KPI degraded beyond threshold between two periods"
    - "detectFatigue handles CPA (up = bad) and ROAS (down = bad) direction correctly"
    - "diagnoseVideoCreative identifies video-specific failure modes (low view rate, missing CTA)"
  artifacts:
    - path: "lib/analysis/gapAnalysis.ts"
      provides: "Gap analysis pure function"
      exports: ["identifyGaps"]
    - path: "lib/analysis/gapAnalysis.test.ts"
      provides: "Gap analysis tests"
      contains: "identifyGaps"
    - path: "lib/analysis/fatigueDetection.ts"
      provides: "Fatigue detection pure function"
      exports: ["detectFatigue"]
    - path: "lib/analysis/fatigueDetection.test.ts"
      provides: "Fatigue detection tests"
      contains: "detectFatigue"
    - path: "lib/analysis/videoAnalysis.ts"
      provides: "Video-specific diagnosis helpers"
      exports: ["diagnoseVideoCreative"]
    - path: "lib/analysis/videoAnalysis.test.ts"
      provides: "Video analysis tests"
      contains: "diagnoseVideoCreative"
  key_links:
    - from: "lib/analysis/gapAnalysis.ts"
      to: "lib/analysis/types.ts"
      via: "CopyTheme, PatternResult type imports"
      pattern: "import.*CopyTheme.*from.*types"
    - from: "lib/analysis/fatigueDetection.ts"
      to: "lib/analysis/types.ts"
      via: "PrimaryKpi type import"
      pattern: "import.*PrimaryKpi.*from.*types"
---

<objective>
TDD the three new pure analysis modules needed for Phase 4: gap analysis (INTL-01), fatigue detection (INTL-02), and video-specific diagnosis helpers (VID-04).

Purpose: These modules must exist before the video page (Plan 04) and briefing page (Plan 05) can use them. Writing tests first ensures the business logic is correct before integration.
Output: 3 tested pure function modules in lib/analysis/
</objective>

<execution_context>
@C:/Users/brent/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/brent/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-multi-format-analysis-and-intelligence/04-RESEARCH.md
@lib/analysis/types.ts
@lib/analysis/patternDetection.ts
@lib/analysis/underperformerDiagnosis.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: TDD gap analysis module (RED then GREEN)</name>
  <files>lib/analysis/gapAnalysis.test.ts, lib/analysis/gapAnalysis.ts</files>
  <action>
**RED phase:** Create `lib/analysis/gapAnalysis.test.ts` with tests for `identifyGaps`:
- Test: given patterns with 4 of 7 themes detected, returns 3 gap results for missing themes
- Test: given all themes detected (all counts >= 1), returns empty array
- Test: given empty detected patterns, returns all 7 themes as gaps
- Test: given minRepresentation=2, themes with count 0 or 1 are returned as gaps
- Test: each gap result has correct theme, label, representation count, and suggestion string

Run tests -- they MUST fail (no implementation yet).

**GREEN phase:** Create `lib/analysis/gapAnalysis.ts` following the exact code pattern from 04-RESEARCH.md:
- Import `CopyTheme` and `PatternResult` from `./types`
- Export `GapResult` interface: `{ theme: CopyTheme; label: string; representation: number; suggestion: string }`
- Define `ALL_THEMES` array with all 7 CopyTheme values
- Define `THEME_LABELS` and `THEME_SUGGESTIONS` Record maps (use AU English spelling)
- Export `identifyGaps(detectedPatterns: PatternResult[], minRepresentation?: number): GapResult[]`
  - Build Map from detectedPatterns (theme -> count)
  - Filter ALL_THEMES where count < minRepresentation (default 1)
  - Return GapResult for each missing/underrepresented theme

Run tests -- they MUST pass.
  </action>
  <verify>
    <automated>npx vitest run lib/analysis/gapAnalysis.test.ts --reporter=verbose</automated>
  </verify>
  <done>identifyGaps correctly identifies themes missing or underrepresented in the portfolio, with all 5 test cases passing</done>
</task>

<task type="auto">
  <name>Task 2: TDD fatigue detection module (RED then GREEN)</name>
  <files>lib/analysis/fatigueDetection.test.ts, lib/analysis/fatigueDetection.ts</files>
  <action>
**RED phase:** Create `lib/analysis/fatigueDetection.test.ts` with tests for `detectFatigue`:
- Test: CPA direction -- creative with CPA increased >20% is flagged as fatigued
- Test: CPA direction -- creative with CPA increased 15% (below 20% threshold) is NOT flagged
- Test: ROAS direction -- creative with ROAS decreased >20% is flagged as fatigued
- Test: ROAS direction -- creative with ROAS decreased 10% (below threshold) is NOT flagged
- Test: creatives below minImpressions threshold are excluded
- Test: creatives only in current period (no prior data) are excluded
- Test: prior period creative with kpiValue=0 is excluded (divide by zero guard)
- Test: empty inputs return empty array

Export types: `CreativeMetrics { adId: string; kpiValue: number; impressions: number }` and `FatiguedCreative { adId: string; currentKpi: number; priorKpi: number; changePercent: number; direction: 'degraded' | 'improved' }`

Run tests -- they MUST fail.

**GREEN phase:** Create `lib/analysis/fatigueDetection.ts` following the research code pattern:
- Export `CreativeMetrics` and `FatiguedCreative` interfaces
- Export `detectFatigue(currentPeriod, priorPeriod, kpiType, degradationThreshold?, minImpressions?): FatiguedCreative[]`
  - Build Map from priorPeriod by adId
  - Filter current period by minImpressions (default 100)
  - For each matching pair, compute changePercent = (current - prior) / prior
  - CPA: positive change > threshold = degraded; ROAS: negative change < -threshold = degraded
  - Skip if prior.kpiValue === 0 or prior.impressions < minImpressions
  - Return array of FatiguedCreative for degraded creatives

Run tests -- they MUST pass.
  </action>
  <verify>
    <automated>npx vitest run lib/analysis/fatigueDetection.test.ts --reporter=verbose</automated>
  </verify>
  <done>detectFatigue correctly identifies creatives with degraded performance between periods, handling both CPA and ROAS direction, with all 8 test cases passing</done>
</task>

<task type="auto">
  <name>Task 3: TDD video analysis helpers (RED then GREEN)</name>
  <files>lib/analysis/videoAnalysis.test.ts, lib/analysis/videoAnalysis.ts</files>
  <action>
**RED phase:** Create `lib/analysis/videoAnalysis.test.ts` with tests for `diagnoseVideoCreative`:

The function takes a video creative's metrics and portfolio averages, returns a diagnosis object.

- Test: high impressions + low view rate (< 0.5x portfolio avg) => diagnosis: 'not_engaging', evidence mentions view rate
- Test: high view rate + low CTR (< 0.5x portfolio avg) => diagnosis: 'missing_cta', evidence mentions CTA
- Test: high impressions + good view rate + low CVR => diagnosis: 'landing_page_disconnect' (same as standard)
- Test: low impressions => diagnosis: 'not_serving' (same as standard)
- Test: all metrics healthy => returns null (no diagnosis needed)

Export types: `VideoCreativeMetrics { adId: string; impressions: number; videoViewRate: number; ctr: number; cvr: number; videoQuartileP100Rate: number }` and `VideoPortfolioAvg { impressions: number; videoViewRate: number; ctr: number; cvr: number }` and `VideoDiagnosis { adId: string; diagnosis: string; evidence: string; recommendedAction: string }`

Run tests -- they MUST fail.

**GREEN phase:** Create `lib/analysis/videoAnalysis.ts`:
- Export the three interfaces above
- Export `diagnoseVideoCreative(creative: VideoCreativeMetrics, portfolioAvg: VideoPortfolioAvg): VideoDiagnosis | null`
  - Decision tree (portfolio-relative thresholds, matching the pattern from diagnoseUnderperformers):
    1. Low impressions (< 0.2x portfolio avg) => 'not_serving'
    2. High impressions + low view rate (< 0.5x avg) => 'not_engaging' (video-specific)
    3. High view rate + low CTR (< 0.5x avg) => 'missing_cta' (video-specific)
    4. High impressions + good metrics + low CVR (< 0.5x avg) => 'landing_page_disconnect'
    5. Otherwise => null
  - Include appropriate evidence strings explaining the diagnosis
  - Include recommendedAction strings with specific suggestions

Run tests -- they MUST pass.
  </action>
  <verify>
    <automated>npx vitest run lib/analysis/videoAnalysis.test.ts --reporter=verbose</automated>
  </verify>
  <done>diagnoseVideoCreative correctly classifies video creatives with video-specific diagnosis paths (not_engaging, missing_cta), with all 5 test cases passing</done>
</task>

</tasks>

<verification>
```bash
npx vitest run lib/analysis/gapAnalysis.test.ts lib/analysis/fatigueDetection.test.ts lib/analysis/videoAnalysis.test.ts --reporter=verbose
```
All 18 tests across 3 test files must pass.

Also run the full suite to confirm no regressions:
```bash
npx vitest run --reporter=verbose
```
</verification>

<success_criteria>
- 3 new pure function modules exist in lib/analysis/ with named exports
- 3 test files with comprehensive edge case coverage
- All tests pass (existing + new)
- No new npm packages installed
- Functions are format-agnostic where possible (gapAnalysis, fatigueDetection) and video-specific where necessary (videoAnalysis)
</success_criteria>

<output>
After completion, create `.planning/phases/04-multi-format-analysis-and-intelligence/04-01-SUMMARY.md`
</output>
