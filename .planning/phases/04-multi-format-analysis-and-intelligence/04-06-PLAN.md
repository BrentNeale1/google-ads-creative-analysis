---
phase: 04-multi-format-analysis-and-intelligence
plan: 06
type: execute
wave: 1
depends_on: []
files_modified:
  - components/briefing/BriefingSection.tsx
autonomous: true
requirements:
  - INTL-03
gap_closure: true

must_haves:
  truths:
    - "Briefing page loads without error and shows 4 intelligence sections"
    - "Each section displays its icon, title, and count badge correctly"
  artifacts:
    - path: "components/briefing/BriefingSection.tsx"
      provides: "Server Component section card for briefing page"
      contains: "export const BriefingSection"
  key_links:
    - from: "app/briefing/page.tsx"
      to: "components/briefing/BriefingSection.tsx"
      via: "imports BriefingSection and passes Lucide icon components as props"
      pattern: "icon.*TrendingUp|AlertTriangle|Lightbulb|ClipboardList"
---

<objective>
Fix the Server/Client boundary error on the Monday Briefing page that prevents it from loading.

Purpose: The briefing page crashes because `BriefingSection.tsx` is marked `"use client"` but receives Lucide icon components (non-serialisable functions) as props from the Server Component `app/briefing/page.tsx`. Since `BriefingSection` uses no client-side features (no hooks, no event handlers, no browser APIs), the fix is to remove the `"use client"` directive so it renders as a Server Component.

Output: A working `/briefing` page that loads all 4 sections without errors.
</objective>

<execution_context>
@C:/Users/brent/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/brent/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-multi-format-analysis-and-intelligence/04-05-SUMMARY.md
@components/briefing/BriefingSection.tsx
@app/briefing/page.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove "use client" from BriefingSection and verify build</name>
  <files>components/briefing/BriefingSection.tsx</files>
  <action>
Remove the `"use client";` directive from the top of `components/briefing/BriefingSection.tsx`. This is the ONLY change needed.

Why this works: `BriefingSection` is a purely presentational component -- it renders an icon, title, count badge, and children slot. It uses no React hooks (`useState`, `useEffect`, etc.), no event handlers (`onClick`, `onChange`), and no browser APIs. It can safely be a Server Component.

Why the children still work: The `children` prop includes `ChangeIndicator` which IS a Client Component (`"use client"`), but React supports passing Client Components as `children` into Server Components. The serialisation boundary is respected because `children` is a valid React element tree, not a raw function reference.

Do NOT change anything else in the file -- no renaming, no restructuring, no prop changes. The `icon: ElementType` prop pattern works correctly in Server Components because the parent (`page.tsx`) is also a Server Component, so no serialisation boundary exists between them.
  </action>
  <verify>
    <automated>cd C:/Users/brent/google-ads-creative-analysis && npm run build 2>&1 | tail -20</automated>
    <manual>Navigate to /briefing?account={seed-account-id} and confirm all 4 sections render: "What Changed", "Needs Attention", "Creative Gaps", "What to Do"</manual>
  </verify>
  <done>
    - `npm run build` completes without errors
    - BriefingSection.tsx has no `"use client"` directive
    - /briefing page loads and renders all 4 sections with icons, titles, and content
    - No "Functions cannot be passed directly to Client Components" error
  </done>
</task>

</tasks>

<verification>
1. `npm run build` passes -- confirms no Server/Client boundary violations at compile time
2. `npm run dev` then visit `/briefing?account={any-seed-account}` -- page loads without runtime errors
3. All 4 BriefingSection cards visible with correct icons (TrendingUp, AlertTriangle, Lightbulb, ClipboardList)
4. ChangeIndicator components inside sections still render correctly (they remain Client Components via their own `"use client"` directive)
</verification>

<success_criteria>
The Monday Briefing page at `/briefing` loads successfully and displays all 4 intelligence sections without any React serialisation errors. UAT test 12 passes.
</success_criteria>

<output>
After completion, create `.planning/phases/04-multi-format-analysis-and-intelligence/04-06-SUMMARY.md`
</output>
