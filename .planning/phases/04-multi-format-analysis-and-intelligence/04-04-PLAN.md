---
phase: 04-multi-format-analysis-and-intelligence
plan: 04
type: execute
wave: 2
depends_on: ["04-01"]
files_modified:
  - lib/queries/video.ts
  - app/video/page.tsx
  - app/video/searchParams.ts
  - components/video/VideoTabNav.tsx
  - components/video/VideoLeaderboard.tsx
  - components/video/VideoEngagementChart.tsx
autonomous: true
requirements:
  - VID-01
  - VID-02
  - VID-03
  - VID-04
  - VID-05
  - VID-06

must_haves:
  truths:
    - "Video creatives are classified into top 20%, middle 60%, bottom 20% tiers by primary KPI"
    - "Video page shows creative leaderboard with video-specific metric columns (view rate, VTR)"
    - "Video page displays video-specific metrics: view rate, average CPV, quartile completion rates"
    - "Video page shows video-specific underperformer diagnosis (not_engaging, missing_cta paths)"
    - "Video page shows Keep/Test/Pause/Investigate recommendations"
    - "Video engagement chart shows quartile completion funnel (P25 -> P50 -> P75 -> P100)"
  artifacts:
    - path: "lib/queries/video.ts"
      provides: "Video data aggregation queries"
      exports: ["fetchVideoCreatives", "fetchVideoPortfolioAvg"]
    - path: "app/video/page.tsx"
      provides: "Video analysis page Server Component"
      exports: ["default"]
    - path: "app/video/searchParams.ts"
      provides: "Video URL search param cache"
      exports: ["videoSearchParams", "resolveDateRange"]
    - path: "components/video/VideoLeaderboard.tsx"
      provides: "Video creative leaderboard with video metrics"
      exports: ["VideoLeaderboard"]
    - path: "components/video/VideoEngagementChart.tsx"
      provides: "Quartile completion funnel chart"
      exports: ["VideoEngagementChart"]
  key_links:
    - from: "app/video/page.tsx"
      to: "lib/queries/video.ts"
      via: "Server-side data fetch"
      pattern: "fetchVideoCreatives"
    - from: "app/video/page.tsx"
      to: "lib/analysis/videoAnalysis.ts"
      via: "Video-specific diagnosis"
      pattern: "diagnoseVideoCreative"
    - from: "app/video/page.tsx"
      to: "lib/analysis/tierClassification.ts"
      via: "classifyTiers pipeline"
      pattern: "classifyTiers"
---

<objective>
Build the Video analysis page with tier classification, video-specific leaderboard (view rate, VTR, CPV), quartile completion funnel, video-specific underperformer diagnosis, and recommendations.

Purpose: Video creatives have unique engagement metrics (view rate, quartile completion, CPV) that require specialised display and diagnosis. The video page uses the videoAnalysis module from Plan 01 for enhanced diagnosis.
Output: Working /video page with 3 tabs (Overview, Engagement, Recommendations) following the RSA page pattern with video-specific adaptations.
</objective>

<execution_context>
@C:/Users/brent/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/brent/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-multi-format-analysis-and-intelligence/04-RESEARCH.md
@.planning/phases/03-rsa-analysis/03-04-SUMMARY.md
@app/rsa/page.tsx
@app/rsa/searchParams.ts
@lib/queries/rsa.ts
@lib/db/schema.ts
@lib/analysis/types.ts
@lib/analysis/videoAnalysis.ts
@components/rsa/TierOverview.tsx
@components/rsa/UnderperformerPanel.tsx
@components/rsa/RecommendationList.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Video query module and search params</name>
  <files>lib/queries/video.ts, app/video/searchParams.ts</files>
  <action>
**Create `lib/queries/video.ts`** following the established query pattern:
- Add `import "server-only"` at top
- Import db, schema, drizzle operators, convertMicrosToAud
- Define `VideoCreativeAggregated` interface: adId, adName (nullable), campaignName, adGroupName, impressions, clicks, costMicros, conversions, conversionsValue, costAud, ctr, cvr, cpa, roas, PLUS video-specific fields: videoViews, videoViewRate, averageCpvMicros, averageCpvAud, videoQuartileP25Rate, videoQuartileP50Rate, videoQuartileP75Rate, videoQuartileP100Rate
- Add `safeDivide(n, d)` helper
- Export `fetchVideoCreatives(accountId, dateFrom, dateTo, campaignId?, adGroupId?)`: aggregate `videoDaily` by adId+adName+campaignName+adGroupName.
  - Sum standard metrics (impressions, clicks, costMicros, conversions, conversionsValue)
  - Sum videoViews
  - For rate fields (videoViewRate, quartile rates, averageCpvMicros), use weighted average: sum(field * impressions) / sum(impressions) -- because rates should be impression-weighted, not simple averaged.
  - Actually, since the daily rows already have the rates, and we want aggregate rates: for videoViewRate = sum(videoViews) / sum(impressions). For quartile rates, use impression-weighted average. For averageCpvMicros, use sum(costMicros) / sum(videoViews).
  - Compute derived metrics (ctr, cvr, cpa, roas, averageCpvAud) with safeDivide.
- Export `fetchVideoPortfolioAvg(accountId, dateFrom, dateTo)`: compute portfolio-level averages including video-specific ones: avgImpressions, avgCtr, avgCvr, avgVideoViewRate (average of per-creative view rates).

**Create `app/video/searchParams.ts`** following the established pattern:
- Define `videoSearchParams` cache with: account, range, from, to, campaign, adGroup, tab (overview/engagement/recommendations)
- Export `resolveDateRange`, `VideoTab`, `DateRange`
  </action>
  <verify>
    <automated>npx tsc --noEmit --pretty 2>&1 | head -30</automated>
  </verify>
  <done>Video query module exports fetchVideoCreatives (with video-specific aggregation logic) and fetchVideoPortfolioAvg. Search params defined for Video page.</done>
</task>

<task type="auto">
  <name>Task 2: Video analysis page with engagement chart and video diagnosis</name>
  <files>app/video/page.tsx, components/video/VideoTabNav.tsx, components/video/VideoLeaderboard.tsx, components/video/VideoEngagementChart.tsx</files>
  <action>
**Create `components/video/VideoTabNav.tsx`** -- "use client" component:
- 3 tabs: Overview, Engagement, Recommendations

**Create `components/video/VideoLeaderboard.tsx`** -- "use client" component:
- Adapted from CreativeLeaderboard for Video:
- First column: ad name (or "Video {adId}" fallback)
- Standard columns: Impr., Clicks, CTR, Conv., CPA/ROAS
- Video-specific columns: View Rate (formatted as percentage), VTR (P100 rate as percentage), CPV (formatted as AUD currency)
- Top 5 / Bottom 5 tables with tier highlighting
- Portfolio average row
- formatKpi created inside component from kpiType prop
- Format view rate and VTR as percentages (1dp), CPV as AUD currency

**Create `components/video/VideoEngagementChart.tsx`** -- "use client" component:
- This is the unique video component showing the quartile completion funnel
- Accepts `creatives: array with quartile rates` and `kpiType` props
- Compute average quartile rates across all creatives (or top tier only)
- Render a **horizontal bar chart** using Recharts showing:
  - 4 bars: "25% watched" (P25), "50% watched" (P50), "75% watched" (P75), "Completed" (P100)
  - Sorted by quartile order (P25 -> P100), naturally decreasing
  - Use design system colours: Primary Blue #1A73E8
  - Data labels showing percentage on each bar
  - Background #F8F9FA, no borders, subtle gridlines
- Below the chart, show a small insight: "Average completion rate: {P100}% — {interpretation}" where interpretation is "strong retention" if P100 > 30%, "typical" if 10-30%, "low completion" if <10%
- Include a comparison row if possible: top tier average vs bottom tier average completion

**Create `app/video/page.tsx`** -- Server Component:
1. Parse videoSearchParams
2. Empty state if no account selected
3. Fetch account for primaryKpi
4. resolveDateRange
5. Fetch in parallel: fetchVideoCreatives, fetchVideoPortfolioAvg, fetchFilterOptions
6. Enrich creatives as CreativeInput[]:
   - adId = c.adId
   - kpiValue = primaryKpi === 'cpa' ? c.cpa : c.roas
   - headlineText = undefined (Video ads don't have headline text)
   - Pass through: all standard metrics + video-specific metrics
7. Run standard analysis pipeline: classifyTiers, generateRecommendations
8. Run video-specific diagnosis: Import `diagnoseVideoCreative` from `lib/analysis/videoAnalysis.ts`. For each bottom-tier creative, call `diagnoseVideoCreative` with the creative's video metrics and portfolio averages. This replaces (or supplements) the generic `diagnoseUnderperformers` for video.
   - Build `VideoCreativeMetrics` from enriched data
   - Build `VideoPortfolioAvg` from fetchVideoPortfolioAvg result
   - Map diagnosed results to the `DiagnosedCreative` interface format for UnderperformerPanel compatibility
9. Render with FilterBar, VideoTabNav, tab content:
   - Overview tab: TierOverview (reused), VideoLeaderboard
   - Engagement tab: VideoEngagementChart with quartile funnel
   - Recommendations tab: UnderperformerPanel (reused), RecommendationList (reused)
10. Export `dynamic = "force-dynamic"`

**Video-specific notes:**
- No pattern charts (Video lacks headline text)
- Video diagnosis uses diagnoseVideoCreative for enhanced video-specific paths (not_engaging, missing_cta)
- Quartile completion funnel is the unique analytical feature
- AU English in all copy
  </action>
  <verify>
    <automated>npx tsc --noEmit --pretty 2>&1 | head -30</automated>
    <manual>Navigate to /video?account={seed-account-id} and verify tier overview, leaderboard with video metrics, engagement chart with quartile funnel, and recommendations render correctly</manual>
  </verify>
  <done>Video page at /video renders tier overview, video leaderboard with view rate/VTR/CPV columns, quartile completion funnel chart, video-specific underperformer diagnosis, and recommendations. All 3 tabs work.</done>
</task>

</tasks>

<verification>
```bash
npx tsc --noEmit --pretty
```
TypeScript compilation passes with no errors.

```bash
npx vitest run --reporter=verbose
```
All tests pass (existing + video analysis tests from Plan 01).

Visual verification: /video page loads with seed data, shows video-specific metrics and quartile funnel.
</verification>

<success_criteria>
- /video page renders with seed data showing video creative analysis
- Leaderboard includes video-specific columns (view rate, VTR, CPV)
- Quartile completion funnel chart shows P25 -> P50 -> P75 -> P100 bars
- Video-specific diagnosis identifies "not_engaging" and "missing_cta" failure modes
- Pattern detection gracefully handles missing headline text
- 3 tabs navigate via URL search param
- No new npm packages
</success_criteria>

<output>
After completion, create `.planning/phases/04-multi-format-analysis-and-intelligence/04-04-SUMMARY.md`
</output>
